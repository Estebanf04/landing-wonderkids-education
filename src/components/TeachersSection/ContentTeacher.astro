---
interface Props {
    src: string,
    alt: string,
    teacherName: string,
    job: string,
}

const {src, alt, teacherName, job} = Astro.props
---

<img 
  src={src} 
  alt={alt}
  class="pb-6"
>
<p class="text-2xl font-semibold pb-2">{teacherName}</p>
<p class="text-lg">{job}</p>