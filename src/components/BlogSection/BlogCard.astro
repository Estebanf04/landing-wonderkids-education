---
import { GoArrowUpRight } from "react-icons/go";
import type { Blog } from "./ReadOurBlog.astro";

interface Props {
    src: string,
    alt: string,
    title: string,
    subtitleFirstLine: string,
    subtitleSecLine: string
}

const { 
    src, 
    alt, 
    title, 
    subtitleFirstLine, 
    subtitleSecLine 
} = Astro.props as Blog

---

<div class="bg-[#F5F5F5] h-[448px] rounded-[48px]">
	<img 
      src={src}
      alt={alt}
    >
	<div class="flex flex-col gap-4 py-6 px-8">
		<p class="text-2xl font-semibold">{title}</p>
		<p class="font-medium text-[#636363]">{subtitleFirstLine} <br> {subtitleSecLine}</p>
		<div class="flex items-center gap-4 cursor-pointer group">
			<p class="text-[#704FE6] font-semibold">Learn more</p>
			<div class="bg-[#704FE6] p-3 rounded-full group-hover:rotate-45 transition-transform duration-300">
				<GoArrowUpRight color="white"/>
			</div>	
		</div>
	</div>
</div>